apiVersion: apps.open-cluster-management.io/v1
kind: Subscription
metadata:
  name: helm-kubernetes-external-secrets
spec:
  channel: mgmt-channel-helm-kubernetes-external-secrets/helm-kubernetes-external-secrets
  placement:
    placementRef:
      name: mycluster
      kind: PlacementRule
