apiVersion: apps.open-cluster-management.io/v1
kind: Subscription
metadata:
  annotations:
    apps.open-cluster-management.io/reconcile-option: replace
  name: external-secrets
  namespace: external-secrets
spec:
  channel: external-secrets-channel/external-secrets
  placement:
    placementRef:
      name: external-secrets
      kind: PlacementRule
