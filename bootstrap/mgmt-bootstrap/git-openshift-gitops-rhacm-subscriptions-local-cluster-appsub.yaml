apiVersion: apps.open-cluster-management.io/v1
kind: Subscription
metadata:
  annotations:
    apps.open-cluster-management.io/github-path: subscriptions/local-cluster
  name: git-openshift-gitops-rhacm-subscriptions-local-cluster
spec:
  channel: git-openshift-gitops-rhacm
  placement:
    placementRef:
      name: mgmt-bootstrap
      kind: PlacementRule
