apiVersion: apps.open-cluster-management.io/v1
kind: Subscription
metadata:
  annotations:
    apps.open-cluster-management.io/github-path: subscriptions/local-cluster
  name: mgmt-bootstrap-replace
spec:
  channel: git-rhacm-kustomization
  placement:
    placementRef:
      name: mgmt-bootstrap
      kind: PlacementRule
